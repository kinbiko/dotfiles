gh pr checks --watch
# Desktop notification on Linux, spoken notification on macOS.
if command -v notify-send >/dev/null 2>&1; then
  notify-send "CI complete" "Continuous integration jobs complete."
elif command -v say >/dev/null 2>&1; then
  say "Continuous integration jobs complete."
fi
